<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
   <a href="<%= song_path(song) %>">
    <li><%= link_to song.title, song_path(song) %></li>
     <%= song.artist_name %> - <%= song.title %>
  <% end %>
</ul>
